# 역할, 책임, 협력

## 서론

> 객체지향 패러다임의 핵심은, `역할` `책임` `협력`

## 협력

### 영화 예매 시스템 돌아보기

객체지향에서, 애플리케이션의 제어 흐름은 하나의 객체에 의해 통제되지 않고 다양한 객체들 사이에 균형 있게 분배되는 것이 일반적이다.

> 이 때, 기능을 구현하기 위해 객체들이 수행하는 상호작용을 `협력`, 수행하는 로직을 `책임`, 수행하는 로직의 집합을 `역할` 이라고 한다.

### 협력
